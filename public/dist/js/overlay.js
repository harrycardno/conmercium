function overlay() {
	el = document.getElementById("overlay");
	el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
}

function login() {
	le = document.getElementById("login");
	le.style.visibility = (le.style.visibility == "visible") ? "hidden" : "visible";
}

